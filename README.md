# flick
single‑screen capture surface backed by an on‑device semantic pipeline that auto‑classifies voice, text, and screenshots into tasks, events, and notes without the user ever touching a folder, tag, or metadata field.
